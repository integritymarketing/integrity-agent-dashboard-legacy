<!--- Toast.stories.mdx -->

import { Meta, Story, Props, Canvas } from "@storybook/addon-docs/blocks";
import { useToast } from "../../hooks/useToast";

<Meta title="useToast Component Introduction" component={ShowToast} />

const ShowToast = ({ message, time, type }) => {
const showToast = useToast()

    return <div>
        <button onClick={() => showToast({ message, time, type })}>Show toast</button>
    </div>

}

# useToast

    This useToast hook is used across the Agent Portal Application to show a toast message

```
   const ShowToast = ({ message, time, type }) => {
      const showToast = useToast()

      return <div>
          <button onClick={() => showToast({ message, time, type })}>Show toast</button>
      </div>
  }
```

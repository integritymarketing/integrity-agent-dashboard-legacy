import { atom } from "recoil";

export const addProviderModalAtom = atom({
  key: "addProviderModalAtom",
  default: false,
});

export const showViewAvailablePlansAtom = atom({
  key: "showViewAvailablePlansAtom",
  default: false,
});